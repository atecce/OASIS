#!/usr/bin/env python

# need this to post to database
import firebase
	
# import sensors by category
from UART import flow_meter

# declare sensor
sensor = carbon_dioxide(5)

# take reading
reading = sensor.read()

# write to temporary file for API
with sense as open('/tmp/S305'): sense.write(reading)

# post to databases
result = firebase.post('/data', reading)
